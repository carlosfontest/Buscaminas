package clases;

// Manipulador que usamos para llamar a la primera interfaz
public class Manipuladorish {

    public Manipuladorish() {

        FrameIngreso frameIngreso = new FrameIngreso();
        frameIngreso.setVisible(true);
        
    }    
}
